# Relay

Relay is the conversational internal developer portal working model in this repository.

Published from repo markdown under `docs/techdocs/relay/` (Backstage TechDocs, Phase 1C.3).

## What to read next

- [Local setup](local-setup.md) — run the stack with Docker Compose (`make up`, chat UI, optional LLM and Backstage).

## Related repo docs

Canonical copies also live at the repo root under `docs/` (roadmap, corpus pipeline, testing). The RAG corpus under `knowledge/corpus/` is indexed by the assistant, not this TechDocs site.

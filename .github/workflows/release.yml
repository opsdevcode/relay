name: Release

# Docs-only merges do not run the release job (see CONTRIBUTING.md).
on:
  push:
    branches: [main]
    paths-ignore:
      - "docs/**"
      - "**/*.md"
      - "LICENSE"
      - ".github/pull_request_template.md"
      - "CHANGELOG.md"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/portal-assistant
    steps:
      - name: Require release token
        env:
          PORTAL_RELEASE_TOKEN: ${{ secrets.PORTAL_RELEASE_TOKEN }}
        run: |
          if [ -z "$PORTAL_RELEASE_TOKEN" ]; then
            echo "::error::PORTAL_RELEASE_TOKEN is not set. Add a maintainer PAT with contents:write — see CONTRIBUTING.md"
            exit 1
          fi

      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
          token: ${{ secrets.PORTAL_RELEASE_TOKEN }}

      - uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b # v5.3.0
        with:
          python-version: "3.12"

      - name: Install portal-assistant and semantic-release
        run: |
          pip install --no-cache-dir -e ".[dev]"
          pip install "python-semantic-release~=10.0"

      - name: Run tests
        env:
          PYTHONPATH: src
        run: python -m pytest -q

      - name: Create GitHub release on conventional commits
        env:
          GH_TOKEN: ${{ secrets.PORTAL_RELEASE_TOKEN }}
        run: semantic-release version

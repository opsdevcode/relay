apiVersion: v1
kind: Service
metadata:
  name: relay-web
  namespace: relay
  labels:
    app.kubernetes.io/name: relay-web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: relay-web
  ports:
    - port: 80
      targetPort: 80

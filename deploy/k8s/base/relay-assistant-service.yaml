apiVersion: v1
kind: Service
metadata:
  name: relay-assistant
  namespace: relay
  labels:
    app.kubernetes.io/name: relay-assistant
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: relay-assistant
  ports:
    - name: http
      port: 8080
      targetPort: http

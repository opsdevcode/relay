apiVersion: v1
kind: Service
metadata:
  name: portal-assistant
  namespace: developer-portal
  labels:
    app.kubernetes.io/name: portal-assistant
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: portal-assistant
  ports:
    - name: http
      port: 8080
      targetPort: http

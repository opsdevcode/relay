# Run from repo root:
#   docker compose up --build -d              # portal stack (postgres, API, web)
#   docker compose --profile backstage up --build -d   # + Backstage :3001 / :7007
#
# Equivalent to: docker compose -f deploy/docker-compose.yml …
include:
  - deploy/docker-compose.yml
